# Upstream collaboration artifacts

Material prepared for the [OpenChamber](https://github.com/openchamber/openchamber)
project, kept here so it is versioned with the connectors.

| File | What it is | Status |
|---|---|---|
| `docs-pr-custom-agent-backends.md` | PR body and submit steps for a docs section on using a different coding agent | branch prepared in a local clone, not pushed |
| `opencode-server-agent-backends.patch` | The same change as a `git format-patch` file (`packages/docs/.../opencode-server.mdx`) | applies to `main` at `66b42b3` |
| `issue-choose-agent-backend.md` | Issue draft for a backend picker in first-launch onboarding | not filed |

Nothing here modifies OpenChamber. The patch is a proposal.
